# simple-app-nested-backend

Test fixture: an app plugin with its own backend and a nested datasource that also has a backend. Used by the act tests to verify that per-platform packaging includes exactly one binary per backend per os/arch zip.
